<%- include('partials/header', { title: 'Login' }) %>

    <div class="container">

        <div class="form-container">
            <h2>Login</h2>

            <form action="/site/login" method="POST">

                <% if (typeof error !=='undefined' && error) { %>
                    <div class="flash-message error" style="margin-bottom: 1rem;">
                        <%= error %>
                    </div>
                    <% } %>

                        <div class="form-group">
                            <label for="email">Email: <span class="required-star">*</span></label>
                            <input type="email" id="email" name="email" class="form-control" required>
                        </div>
                        <div class="form-group">
                            <label for="password">Senha: <span class="required-star">*</span></label>
                            <div class="password-wrapper">
                                <input type="password" id="password" name="password" class="form-control" required>
                                <button type="button" class="password-toggle">👁️</button>
                            </div>
                        </div>

                        <button type="submit" class="btn-secondary">Entrar</button>
            </form>

            <p class="form-link">Não tem uma conta? <a href="/site/register">Cadastre-se</a></p>
        </div>

    </div>

    <%- include('partials/footer') %>