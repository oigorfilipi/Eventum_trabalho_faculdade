<%- include('partials/header', { title: 'Nossos Eventos' }) %>

    <div class="container">
        <% if (message) { %>
            <div class="flash-message <%= message.type %>">
                <%= message.text %>
            </div>
            <% } %>

                <h1>Próximos Eventos</h1>

                <div class="event-list">

                    <% events.forEach(event=> { %>
                        <div class="event-card">
                            <h3>
                                <%= event.title %>
                            </h3>
                            <%# --- INÍCIO DO NOVO BLOCO DE DETALHES --- %>
                                <div class="event-details">
                                    <% if (event.schedule) { %>
                                        <div class="detail-item">
                                            🗓️ <strong>Início:</strong>
                                            <%= event.schedule.date_start ? new
                                                Date(event.schedule.date_start).toLocaleDateString('pt-BR',
                                                {timeZone: 'UTC' }) : 'Data' %>
                                                <%= event.schedule.time_start ? ` às ${event.schedule.time_start}` : ''
                                                    %>
                                        </div>
                                        <% if (event.schedule.date_end) { %>
                                            <div class="detail-item">
                                                🗓️ <strong>Fim:</strong>
                                                <%= new Date(event.schedule.date_end).toLocaleDateString('pt-BR',
                                                    {timeZone: 'UTC' }) %>
                                                    <%= event.schedule.time_end ? ` às ${event.schedule.time_end}` : ''
                                                        %>
                                            </div>
                                            <% } %>
                                                <% } %>

                                                    <% if (event.pricing && event.pricing.isFree==='false' ) { %>
                                                        <div class="detail-item">
                                                            💰 <strong>Preço:</strong>
                                                            <%= event.pricing.price || 'Consultar valor' %>
                                                        </div>
                                                        <% } else if (event.pricing) { %>
                                                            <div class="detail-item">
                                                                💰 <strong>Preço:</strong> Gratuito
                                                            </div>
                                                            <% } %>
                                </div>
                                <%# --- FIM DO NOVO BLOCO DE DETALHES --- %>
                                    <p>
                                        <%= event.description || 'Este evento não possui descrição.' %>
                                    </p>

                                    <% if (user) { %>
                                        <% if (user.role==='admin' ) { %>
                                            <div class="event-card-actions">
                                                <a href="/site/eventos/<%= event.id %>/editar" class="btn">Editar</a>
                                                <a href="/site/eventos/<%= event.id %>/excluir"
                                                    class="btn btn-danger">Excluir</a>
                                            </div>
                                            <% } else { %>
                                                <form action="/subscribe" method="POST">
                                                    <input type="hidden" name="eventId" value="<%= event.id %>">
                                                    <input type="hidden" name="userId" value="<%= user.id %>">
                                                    <button type="submit" class="btn">Inscrever-se</button>
                                                </form>
                                                <% } %>
                                                    <% } else { %>
                                                        <a href="/site/login" class="btn">Login para se inscrever</a>
                                                        <% } %>
                        </div>
                        <% }) %>

                </div>
    </div>

    <%- include('partials/footer') %>